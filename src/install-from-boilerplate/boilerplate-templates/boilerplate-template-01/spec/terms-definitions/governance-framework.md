[[tref: ToIPTechArch, governance framework]]

~ *Note: This is a **tref** example — the definition of "governance framework" is imported from the ToIP Technical Architecture Specification rather than defined locally. Use `tref` when you want to reuse the exact definition from another specification. Replace `ToIPTechArch` with the external spec key configured in your `specs.json`.*

~ See also: [[ref: trust registry]], [[xref: ToIPTechArch, trust community]].
